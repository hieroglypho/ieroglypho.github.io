# OS / filesystem junk
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/

# Backups & temp
*.bak
*.tmp
*.orig

# Editor PDF exports — the in-app export drops canvas_<timestamp>.pdf into the
# working dir; transient artifacts, not part of the site
canvas_*.pdf

# Paper working drafts (the published .docx is tracked; LibreOffice .odt is a local draft)
Hieroglyphica-paper.odt
.~lock.*#

# YouTube tutorial scripts / plan — local authoring docs, not part of the site
tutorial/

# Dictionary-source evaluation ledger — local research notes, not part of the site
DICTIONARY-SOURCES.local.md
# Local research/handoff notes
*.local.md

# Wikidata/TLA dictionary-expansion scaffolding — regenerable working files
prototype-wikidata/

# Cloudflare Wrangler local cache for the share-worker (regenerable; also holds
# a cached account-credentials file that must never be committed)
share-worker/.wrangler/
